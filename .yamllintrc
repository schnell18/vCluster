extends: default

rules:
  # 120 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 140
    level: warning

  document-start:
    present: false
    level: warning

  truthy: false
